set(COMMANDS_SOURCE_DIRECTORY "${CODE_DIR}/src/commands")
set(COMMUNICATION_SOURCE_DIRECTORY "${CODE_DIR}/src/communication")
set(CONTAINERS_SOURCE_DIRECTORY "${CODE_DIR}/src/containers")
set(DATABASE_SOURCE_DIRECTORY "${CODE_DIR}/src/database")
set(FASTPROP_SOURCE_DIRECTORY "${CODE_DIR}/src/fastprop")
set(ENGINE_SOURCE_DIRECTORY "${CODE_DIR}/src/engine")
set(HELPERS_SOURCE_DIRECTORY "${CODE_DIR}/src/helpers")
set(IO_SOURCE_DIRECTORY "${CODE_DIR}/src/io")
set(MEMMAP_SOURCE_DIRECTORY "${CODE_DIR}/src/memmap")
set(METRICS_SOURCE_DIRECTORY "${CODE_DIR}/src/metrics")
set(PREDICTORS_SOURCE_DIRECTORY "${CODE_DIR}/src/predictors")
set(SQLITE3_SOURCE_DIRECTORY "${CODE_DIR}/src/sqlite3")
set(TEXTMINING_SOURCE_DIRECTORY "${CODE_DIR}/src/textmining")
set(TRANSPILATION_SOURCE_DIRECTORY "${CODE_DIR}/src/transpilation")
set(TSINDEX_SOURCE_DIRECTORY "${CODE_DIR}/src/tsindex")

include(${COMMANDS_SOURCE_DIRECTORY}/obj.txt)
include(${COMMUNICATION_SOURCE_DIRECTORY}/obj.txt)
include(${CONTAINERS_SOURCE_DIRECTORY}/obj.txt)
include(${DATABASE_SOURCE_DIRECTORY}/obj.txt)
include(${FASTPROP_SOURCE_DIRECTORY}/obj.txt)
include(${ENGINE_SOURCE_DIRECTORY}/obj.txt)
include(${HELPERS_SOURCE_DIRECTORY}/obj.txt)
include(${IO_SOURCE_DIRECTORY}/obj.txt)
include(${MEMMAP_SOURCE_DIRECTORY}/obj.txt)
include(${METRICS_SOURCE_DIRECTORY}/obj.txt)
include(${PREDICTORS_SOURCE_DIRECTORY}/obj.txt)
include(${SQLITE3_SOURCE_DIRECTORY}/obj.txt)
include(${TEXTMINING_SOURCE_DIRECTORY}/obj.txt)
include(${TRANSPILATION_SOURCE_DIRECTORY}/obj.txt)
include(${TSINDEX_SOURCE_DIRECTORY}/obj.txt)

add_executable(
 	${GETML_VERSION}-engine
 	${COMMANDS_OBJ}
 	${COMMUNICATION_OBJ}
 	${CONTAINERS_OBJ}
 	${DATABASE_OBJ}
 	${ENGINE_OBJ}
    ${FASTPROP_OBJ}
    ${HELPERS_OBJ}
    ${IO_OBJ}
 	${METRICS_OBJ}
 	${MEMMAP_OBJ}
	${PREDICTORS_OBJ}
 	${SQLITE3_OBJ}
    ${TEXTMINING_OBJ}
 	${TRANSPILATION_OBJ}
    ${TSINDEX_OBJ}
 	${ENGINE_SOURCE_DIRECTORY}/main.cpp
)
